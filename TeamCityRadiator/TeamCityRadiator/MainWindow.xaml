<Window x:Class="TeamCityRadiator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Teamcity Radiator" Height="350" Width="935">
    <Grid>
        <StackPanel HorizontalAlignment="Left" Height="122" Margin="10,0,0,0" VerticalAlignment="Top" Width="497">
            <Label Content="Type teamcity address:" Margin="0,5"/>
            <TextBox x:Name="AddressBox" Height="23" TextWrapping="Wrap" Text="http:\\localhost:80" Margin="0,5"/>
            <Button Content="Connect..." Margin="25,5" Height="35" Click="Button_Click_1"/>
        </StackPanel>
        <StackPanel HorizontalAlignment="Left" Height="183" Margin="10,127,0,0" VerticalAlignment="Top" Width="497">
            <Label Content="Projects:" Margin="0,5"/>
            <ListBox x:Name="ProjectsListBox" Height="115">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <CheckBox Name="CheckBoxZone" Content="{Binding Path=Name}" IsChecked="{Binding Path= IsSelected}" Checked="CheckBox_Checked" Unchecked="CheckBox_Unchecked" Margin="0,5,0,0"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <Button x:Name="ApplyChangesButton" Content="ApplyChanges" Margin="0,5" Click="Button_Click_2" IsEnabled="False"/>
        </StackPanel>
        <DockPanel Height="305"  Width="395" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Label Content="Build Types:" HorizontalAlignment="Left" VerticalAlignment="Top" RenderTransformOrigin="0.737,0.885" Margin="5"/>
            <Lis
            
          </Grid>
</Window>
